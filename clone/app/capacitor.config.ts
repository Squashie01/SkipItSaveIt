import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'skipitsaveit.app',
  appName: '\x16\x16Skip It Save It',
  webDir: 'build'
};

export default config;
